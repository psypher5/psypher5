{
  "id": "7oLgEV9ih1M",
  "title": "How to Fix the Bloom in Three.js Using TSL and R3F",
  "channel": "Yoa 3D",
  "published": "5 months ago",
  "views": "586 views",
  "length": "8:08",
  "query": "three.js webgpu tsl",
  "word_count": 1570,
  "snippets_count": 226,
  "transcript": "The bloom threshold in 3JS has a hard cutoff. [music] So, let's fix that by making it soft with TSL and React 3 fiber. Why not use the bloom from the post-processing library? It has softness, which is great, but because of how it's implemented, if you have materials with the same color, you can't make one glow and the other not. So, [music] we're going to customize the Bloom add-on that 3JS comes with. The first thing I'm going to do is to set up an R3F canvas [music] with the web GPU renderer and pass it a scene component. We want something simple to help us visualize the bloom. So, I add a cube and some fog, which gives us some variation in brightness to work with. I'm also going to stub out the bloom and pass it a render pipeline, which we'll define shortly. [music] In case that sounds unfamiliar, it used to be called post-processing before version 183. Anyways, in our hook, we're going to instantiate the class, pass it a renderer, and manage its lifetime with use effect, setting [music] a ref, which we return and handling resource cleanup as well. Then we add the use frame hook to call the render method on every frame as long as our render pipeline is defined. And now we're ready to work on our bloom. I'm going to tackle this in [music] three steps. First, we're going to set up the default bloom from three/ add-ons. Then [music] second, we're going to add softness to the luminance threshold. And third, I'll set up a custom material to apply the bloom selectively. [music] I find small customizations like this to be a great way to learn, and I hope you find it useful, too. In order to set up the Bloom, we want to import the node, wrap it in a React hook, and [music] connect it to our scene. We create that connection using a pass node, which takes our scene and camera. It gives us a texture containing the whole scene, which we can pass to the bloom node. Then we take [music] our bloomed output and add it back to our original output to get our combined scene. [music] But we want to use sliders to control our bloom parameters. So, there's some additional work to do. Let's wrap our Bloom setup in a function that returns uniforms along with the node we created. Now we can set those uniforms using values from a state store and update [music] them whenever the sliders change. Here's how I define the values and setters for the bloom radius, strength, and threshold. But we still need to connect it to our actual node. So let's do that. The get bloom node function takes our scene output texture from earlier, sets default values for our uniforms, [music] and calls another function where we add our bloom to our output and connect the uniforms. Add state store values for changing the color and toggling the wireframe. And we have version one of our bloom. The benefit to doing things this [music] way is that our bloom node is self-contained, meaning we can make changes to how it works without affecting other parts of our code. But before we do that, let's go over what changes we need to make in order to add softness to the luminance threshold. The basic idea behind how bloom [music] works is that we take our scene output, filter it based on luminance or brightness, [music] then apply a blur and glow to the remaining parts. So to create our soft bloom effect, what we need to do is to add a buffer around that luminance threshold [music] where we transition from affected to unaffected. How do we determine luminance? We can calculate it by taking the dotproduct of our textures RGB output and a vector with weights representing [music] how sensitive our eyes are to red, green, and blue light. Then we pass in a new uniform called knee, which represents the size of the buffer we want above [music] and below our luminance threshold. Between those upper and lower bounds, we want to create a gradient of values. And the easiest way to do that is [music] to use smooth step. So we pass in our three parameters, the knee start, knee end, and luminance [music] to generate a mask, which is a softer version of our luminance texture. If we multiply our original texture by this new soft mask and pass it to the 3JS bloom [music] node, that's how we can get our soft bloom. And because we're applying the threshold outside of the add-on, we no longer need to connect the bloom nodes threshold [music] value to our threshold uniform, but we do need to make sure to add our knee value wherever we're using our uniforms or state store values to make sure that everything stays fully interactive. Let's add a second cube to the [music] scene and work on applying the bloom selectively to one but not the other. The classic approach which is also mentioned in the post-processing library [music] is to use the emissive intensity to push color values outside of the 0 to 255 range into the HDR space and to set the luminance threshold such that colors in the normal range will not receive bloom and anything above will. But the problem with this method is that you can't have two objects sharing the same color and only selectively apply bloom to one of them. It also prevents you from using tone mapping for [music] the colors in your scene because it clamps your colors to the standard dynamic range from 0 to 255. The first solution I explored was to add a second camera to the scene which only sees some objects, the ones we want to be affected by bloom. You'll see why that solution wasn't sufficient, but I'm sharing it because the core idea behind it applies to the solution that I did end up going with. We start by placing one object on a separate layer. We're going to treat layer 1 as the bloom layer. Then we create a second camera by cloning the default one. Our goal [music] is to make it so that it only sees the bloom layer. We use the pass node again to take the output of this second camera [music] and pass it to the bloom node. We take the output of this operation and add it to our original scene texture [music] to get the result that we're looking for. But we still need to configure the camera. We can do that with the use frame hook where we can set the camera position on every frame and also tell the camera to look at layer [music] 1. You need to do this even though we define the bloom camera by cloning the default one because [clears throat] that's not enough to tell the camera to update its position. Now, at first glance, the bloom looks fine, but if you look closely, you'll notice that there's an issue with [music] the depth. We could solve this by creating more textures and writing depth information to them. [music] But thankfully, we don't have to do that. And there's an easier way. Instead of using a second camera and pass node to render a copy of our scene, we can handle everything in the original scene pass using something called MRT or multiple render targets. [music] So we can safely delete our bloom pass and call the [music] set MRT method on the scene pass instead. This takes an MRT node and output [music] that we import from 3/Tsl and also takes a string key for any render targets that we want to define. In our case, bloom. And I set the value to zero or one depending on whether or not I want to enable bloom. For the overall scene, we want to disable bloom. So I pass zero as a const [music] node. Now we can access a separate texture for our bloom layer by calling [music] scene path.get texture node. the same way we do for our overall scene output. [music] Now, we're going to take both of these texture layers and pass them to our bloom node. And where we call the bloom add-on, we want to multiply our scene texture [music] by the bloom mask so that only objects written to the bloom layer are affected. So, how do we write to our bloom mask? Because we're not able to use layers as we were before. The answer is to create a material and [music] to set its MRT node where we can tell it to write to an additional render target. So our object will write to both the scene output and the bloom layer. For convenience, we can return this material from our use bloom hook. In React 3 fiber, [music] the way to use this custom material in our mesh is to use a primitive in the render [music] tree. Our custom bloom is working nicely. Now we have our soft transition on the luminance [music] threshold, selectivity even if materials share the exact same color, and no depth [music] issues. I used this effect in a stylized flame shader. Check it out if you're interested."
}